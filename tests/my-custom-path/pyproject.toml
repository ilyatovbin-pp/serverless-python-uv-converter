[project]
name = "my-custom-path"
version = "0.1.0"
description = "A generic pyproject.toml"
dependencies = [
    "requests==2.31.0",
    "boto3==1.34.0"
]

[project.optional-dependencies]
dev = [
    "pytest==8.0.0"
]
aws = [
    "requests-aws4auth>=1.3.1"
]
azure = [
    "azure"
]

[dependency-groups]
dev = [
    "pytest==8.0.0"
]
